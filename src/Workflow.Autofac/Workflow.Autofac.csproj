<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>

        <PackageId>Workflow.Autofac</PackageId>

        <Title>Workflow.Autofac</Title>
        <Authors>byCrookie</Authors>
        <PackageTags>workflow;dependency;autofac</PackageTags>
        <Description>A dependency injection extension for workflow to register all components using autofac</Description>

        <RepositoryType>git</RepositoryType>
        <PackageProjectUrl>https://github.com/byCrookie/Workflow</PackageProjectUrl>
        <RepositoryUrl>https://github.com/byCrookie/Workflow</RepositoryUrl>

        <Copyright>https://github.com/byCrookie/Workflow/blob/main/LICENSE</Copyright>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Autofac" Version="6.3.0" />
        <PackageReference Include="buildsdk" Version="1.0.2-pre-20220130032840" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\Workflow\Workflow.csproj" PrivateAssets="all" />
    </ItemGroup>

</Project>
